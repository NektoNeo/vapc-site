# 🔍 Диагностика проблемы с баннерами

## ✅ Что было исправлено:

1. **Добавлено состояние загрузки** - теперь компонент не скрывается до завершения загрузки
2. **Улучшено логирование** - подробные логи на каждом этапе
3. **Добавлена валидация данных** - проверка что баннеры имеют изображения
4. **Исправлена сортировка** - корректное преобразование position в число
5. **Добавлены стили** для `.newsBlock` в `App.module.scss`

## 🔍 Как проверить в браузере:

### 1. Откройте консоль браузера (F12)

Вы должны увидеть следующие логи:

```
🔧 API Configuration: {
  API_BASE_URL: "...",
  NODE_ENV: "...",
  REACT_APP_API_URL: "...",
  currentOrigin: "..."
}

🎬 NewsBlock: компонент смонтирован, начинаем загрузку баннеров
🔍 Запрос баннеров: [URL]
📡 Ответ сервера: 200 OK
📦 Получены баннеры (сырые данные): [...]
✅ Валидных баннеров: X из Y
✅ Данные баннеров получены из базы данных: X шт.
✅ NewsBlock: рендерим X баннеров
```

### 2. Проверьте вкладку Network (F12 → Network)

- Найдите запрос к `/getBanners/` или полный URL
- Проверьте:
  - **Status**: должен быть `200 OK`
  - **Response**: должен содержать массив баннеров
  - **Headers**: проверьте CORS заголовки

### 3. Возможные проблемы:

#### ❌ Проблема: CORS ошибка
```
Access to fetch at '...' from origin '...' has been blocked by CORS policy
```
**Решение**: Проверьте что на сервере настроен CORS для вашего домена

#### ❌ Проблема: 404 Not Found
```
HTTP error! status: 404
```
**Решение**: Проверьте что URL правильный и сервер работает

#### ❌ Проблема: Пустой массив
```
⚠️ NewsBlock: нет баннеров для отображения после загрузки
```
**Решение**: Проверьте что в базе данных есть баннеры с полем `image`

#### ❌ Проблема: Блок скрыт на мобильных
На экранах меньше 768px блок скрыт через CSS (`display: none`). Это нормально.

## 🛠️ Что проверить на сервере:

```bash
# Проверка что API работает
curl http://localhost:8080/getBanners/

# Проверка CORS заголовков
curl -I -X OPTIONS http://localhost:8080/getBanners/ \
  -H 'Origin: https://vasinayw.beget.app' \
  -H 'Access-Control-Request-Method: GET'

# Проверка статуса PM2
pm2 status vapc
pm2 logs vapc --lines 20
```

## 📋 Текущая конфигурация:

- **API URL**: Настраивается через `REACT_APP_API_URL` или относительные пути в production
- **CORS**: Настроен для доменов:
  - `https://vasinayw.beget.app`
  - `https://va-pc.ru`
  - `http://localhost:3000`
  - `http://127.0.0.1:3000`

## 🎯 Следующие шаги:

1. Откройте сайт в браузере
2. Откройте консоль (F12)
3. Скопируйте все логи связанные с баннерами
4. Проверьте вкладку Network на наличие запросов к `/getBanners/`
5. Сообщите что видите в логах

