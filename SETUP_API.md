# Настройка подключения к API на Beget

## Быстрый старт

### 1. Создайте файл `.env` в корне проекта

Скопируйте `.env.example` в `.env` и укажите URL вашего бэкенда:

```bash
# Windows PowerShell
Copy-Item .env.example .env

# Linux/Mac
cp .env.example .env
```

### 2. Укажите URL вашего API в `.env`

```env
REACT_APP_API_URL=https://your-domain.beget.app
```

**Важно:** 
- Не добавляйте слеш в конце URL
- Используйте `https://` для production
- Для локальной разработки можно использовать `http://localhost:PORT`

### 3. Перезапустите dev сервер

После изменения `.env` нужно перезапустить сервер:

```bash
npm start
```

## Как это работает

### В режиме разработки (development)
- Если указан `REACT_APP_API_URL` - используется он
- Если не указан - используются относительные пути (для локального бэкенда)

### В production (на Beget)
- API запросы идут на тот же домен (относительные пути)
- Если нужно указать другой домен - используйте переменную окружения

## Примеры конфигурации

### Вариант 1: Бэкенд на том же домене (рекомендуется для Beget)
```env
# Оставьте пустым или не создавайте .env
# API будет работать через относительные пути
```

### Вариант 2: Бэкенд на отдельном поддомене
```env
REACT_APP_API_URL=https://api.your-domain.beget.app
```

### Вариант 3: Локальная разработка с удаленным API
```env
REACT_APP_API_URL=https://your-domain.beget.app
```

## Проверка подключения

1. Откройте консоль браузера (F12)
2. При загрузке страницы вы увидите:
   ```
   🔧 API Configuration: {
     API_BASE_URL: "https://your-domain.beget.app",
     NODE_ENV: "development",
     REACT_APP_API_URL: "https://your-domain.beget.app"
   }
   ```
3. При успешной загрузке данных:
   ```
   ✅ Данные сборок получены из базы данных: 37 шт.
   ✅ Данные баннеров получены из базы данных: 5 шт.
   ```

## Настройка CORS на бэкенде

Если бэкенд на другом домене, убедитесь что CORS настроен правильно:

```javascript
// Пример для Express.js
app.use(cors({
  origin: ['https://your-frontend-domain.beget.app', 'http://localhost:3000'],
  credentials: true
}));
```

## Все API endpoints

Приложение использует следующие endpoints:

- `GET /getProducts/` - получение списка сборок
- `GET /getBanners/` - получение баннеров
- `GET /getFilters/` - получение фильтров
- `POST /sendMail/` - отправка формы обратной связи
- `POST /authorization/` - авторизация в админ-панели
- `GET /isAuth/` - проверка авторизации
- `POST /addNewProduct/` - добавление новой сборки
- `PUT /updateProductById/` - обновление сборки
- `DELETE /deleteProduct/` - удаление сборки
- `PUT /editProducts/` - массовое редактирование сборок
- `POST /addNewBanner/` - добавление баннера
- `PUT /updateBannerById/` - обновление баннера
- `DELETE /deleteBanner/` - удаление баннера
- `POST /addNewFilter/` - добавление фильтра
- `DELETE /deleteFilter/` - удаление фильтра

## Troubleshooting

### Проблема: CORS ошибка
**Решение:** Настройте CORS на бэкенде или используйте proxy

### Проблема: 404 Not Found
**Решение:** Проверьте правильность URL в `.env` и что бэкенд запущен

### Проблема: Данные не загружаются
**Решение:** 
1. Проверьте консоль браузера на наличие ошибок
2. Проверьте Network tab в DevTools
3. Убедитесь что бэкенд отвечает на запросы

### Проблема: Fallback на статические данные
**Решение:** Это нормально при недоступности API. Проверьте подключение к бэкенду.

