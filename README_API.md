# 🔌 Подключение к API на Beget

## ✅ Что было сделано

1. **Создана централизованная конфигурация API** (`src/config/api.js`)
2. **Обновлены все fetch запросы** для использования конфигурации
3. **Добавлена обработка ошибок** во всех компонентах
4. **Добавлен fallback** на статические данные для сборок

## 🚀 Быстрая настройка

### Шаг 1: Создайте файл `.env`

В корне проекта создайте файл `.env` и укажите URL вашего бэкенда:

```env
REACT_APP_API_URL=https://your-domain.beget.app
```

**Важно:** 
- Замените `your-domain.beget.app` на ваш реальный домен
- Не добавляйте слеш в конце URL
- Для локальной разработки можно использовать `http://localhost:PORT`

### Шаг 2: Перезапустите dev сервер

```bash
npm start
```

### Шаг 3: Проверьте подключение

Откройте консоль браузера (F12) и проверьте:
- Должно появиться сообщение: `🔧 API Configuration: {...}`
- При успешной загрузке: `✅ Данные сборок получены из базы данных`

## 📋 Варианты конфигурации

### Вариант 1: Бэкенд на том же домене (рекомендуется)

Если фронтенд и бэкенд на одном домене Beget:

```env
# Оставьте пустым или не создавайте .env
# API будет работать через относительные пути
```

### Вариант 2: Бэкенд на отдельном поддомене

```env
REACT_APP_API_URL=https://api.your-domain.beget.app
```

### Вариант 3: Локальная разработка с удаленным API

```env
REACT_APP_API_URL=https://your-domain.beget.app
```

## 🎨 Возможности редактирования

Теперь вы можете:

1. **Редактировать сборки** через `/admin-panel`
   - Добавлять новые сборки
   - Редактировать существующие
   - Удалять сборки
   - Загружать изображения

2. **Управлять баннерами** через `/admin-panel/banners`
   - Добавлять баннеры
   - Настраивать порядок отображения
   - Удалять баннеры

3. **Настраивать фильтры** через `/admin-panel/filters`
   - Добавлять ценовые фильтры
   - Удалять фильтры

## 🔍 Отладка

### Проверка конфигурации

В консоли браузера при запуске dev сервера вы увидите:

```javascript
🔧 API Configuration: {
  API_BASE_URL: "https://your-domain.beget.app",
  NODE_ENV: "development",
  REACT_APP_API_URL: "https://your-domain.beget.app"
}
```

### Проверка запросов

1. Откройте DevTools (F12)
2. Перейдите на вкладку **Network**
3. Обновите страницу
4. Проверьте запросы к API endpoints

### Типичные проблемы

#### ❌ CORS ошибка
**Решение:** Настройте CORS на бэкенде:
```javascript
app.use(cors({
  origin: ['https://your-frontend-domain.beget.app', 'http://localhost:3000'],
  credentials: true
}));
```

#### ❌ 404 Not Found
**Решение:** 
- Проверьте правильность URL в `.env`
- Убедитесь что бэкенд запущен
- Проверьте что endpoints существуют на бэкенде

#### ⚠️ Fallback на статические данные
**Решение:** Это нормально при недоступности API. Проверьте:
- Подключение к интернету
- Работает ли бэкенд сервер
- Правильность URL в `.env`

## 📝 Все API endpoints

| Метод | Endpoint | Описание |
|-------|----------|----------|
| GET | `/getProducts/` | Получение списка сборок |
| GET | `/getBanners/` | Получение баннеров |
| GET | `/getFilters/` | Получение фильтров |
| POST | `/sendMail/` | Отправка формы обратной связи |
| POST | `/authorization/` | Авторизация в админ-панели |
| GET | `/isAuth/` | Проверка авторизации |
| POST | `/addNewProduct/` | Добавление новой сборки |
| PUT | `/updateProductById/` | Обновление сборки |
| DELETE | `/deleteProduct/` | Удаление сборки |
| PUT | `/editProducts/` | Массовое редактирование сборок |
| POST | `/addNewBanner/` | Добавление баннера |
| PUT | `/updateBannerById/` | Обновление баннера |
| DELETE | `/deleteBanner/` | Удаление баннера |
| POST | `/addNewFilter/` | Добавление фильтра |
| DELETE | `/deleteFilter/` | Удаление фильтра |

## 🎯 Следующие шаги

1. ✅ Создайте `.env` файл с URL вашего бэкенда
2. ✅ Перезапустите dev сервер
3. ✅ Проверьте подключение в консоли браузера
4. ✅ Протестируйте редактирование через админ-панель

## 📚 Дополнительная информация

Подробная документация: см. `SETUP_API.md`

